Si vogliono tutti vincoli di uguaglianza
- $\leq$ $\rightarrow$ ( + ) variabile di slack $=$
- $\geq$ $\rightarrow$ ( - ) variabile di slack $=$

Se variabili negative: inverto segno e predicato ???

FO: se $\max z\,\rightarrow \min -z$ 
